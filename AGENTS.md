# 公众号排版设计师 — Agent 开发守则

## 产品定义

这不是 Markdown 换皮工具，而是公众号内容设计工作台。系统必须先理解文章结构，再完成语义编排、品牌化设计、微信兼容检查和交付。

核心主张：不是替文章换颜色，而是替内容建立秩序。

## 不可违背的原则

1. 不复制 Punk微排、Doocs、mdnice 或其他工具的页面结构、配色、组件造型与品牌语言。
2. 可以学习成熟项目的功能边界、兼容策略和发布流程，但必须用本产品自己的交互模型重新实现。
3. 前端保持“中文编辑设计室”视觉：深蓝工作台、中国红行动色、瓷白承载面、宋体内容、无衬线控制界面。
4. 禁止 emoji。图标使用统一的线性 SVG 图标。
5. 功能不能静默失败。不支持的 Markdown、图片、链接或微信样式必须明确提示。
6. 预览漂亮不等于交付成功。所有发布功能以粘贴进微信公众号后台后的结果为准。
7. AppSecret、Access Token 等凭据不得存放在浏览器前端、日志或源码中。

## 产品架构

输入层 → Article AST → Design Engine（Tokens / Typography / Components）→ Theme DSL → Platform Adapter → 微信内联 HTML / 复制交付。

主题只包含设计令牌、字阶和组件映射，不得复制解析逻辑。每种主题必须复用同一个内容 AST 与兼容编译器。浅色与深色都必须按语义令牌映射，禁止用整页反相滤镜冒充深色模式。

## 当前阶段

当前为 V0.1 高保真工作台：

- 稿件工作流
- 手机/桌面画布
- Markdown 原稿编辑
- 内容结构识别
- 智能编排建议
- 5 套职责明确的 Markdown 版式
- 品牌套件
- 微信兼容检查
- 富文本复制
- 本地自动保存

## P0 开发顺序

1. Markdown AST 与支持语法清单
2. 脚注、表格、代码块、图片、链接兼容策略
3. 微信粘贴回归测试
4. 不支持语法预警
5. 草稿与版本历史
6. 素材中心
7. 品牌组件库
8. 安全的公众号草稿箱交付

## 前端规则

- 工作界面首屏必须直接暴露稿件、画布和设计建议，禁止营销 Hero。
- 颜色、字号、行距通过 CSS 变量或 ThemeToken 驱动。
- 页面在 1280px 桌面、1024px 小屏桌面、390px 手机宽度均须可用。
- 所有按钮提供键盘焦点状态和可访问名称。
- 操作反馈控制在 120—320ms，页面转场不超过 560ms；环境动效可以更慢，但必须支持 `prefers-reduced-motion`。
- 不堆叠渐变卡片、发光边框、玻璃拟态和无意义数据。
- 文案使用真实公众号生产语境，不用“AI赋能创作”等空话。

## 数据与状态

- 浏览器存储只用于设备本地稿件和偏好。
- 所有本地数据结构必须带 `schemaVersion`，后续升级必须提供迁移。
- 自动保存应防抖，并保留最近恢复点。
- 云端草稿、协作和发布记录进入持久化存储后，须保留审计字段。

## 交付测试

每次涉及解析、主题或复制逻辑的改动，至少验证：

- 标题、段落、引用、列表、图片、链接、表格、代码块数量不丢失
- 公众号粘贴后没有依赖 class 或 `<style>` 标签
- 深色预览开启时，复制结果仍为原主题浅色语义
- 超宽表格、失效图片、空链接有明确告警
- 移动端没有横向溢出
- 原稿刷新后可恢复
- 复制成功与失败均有明确反馈
