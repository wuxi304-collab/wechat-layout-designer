# Mars Editor 设计审计与原创转译

参考仓库：`whyubel1eve/Mars-Editor-APP`，审计日期：2026-08-25。

## 值得学习的设计角度

1. **界面外观与文章主题分离**：编辑器深浅色不应污染交付正文，文章主题单独进入微信内联样式编译。
2. **工作视图有明确模式**：校订与成稿不是两个临时按钮，而是稳定、可感知、可切换的工作状态。
3. **低频选择按需展开**：主题与密度不长期占据工作区；用排版入口展开真实小样，选择后回到写作。
4. **主题卡必须展示结构**：小样需要表达标题装饰、正文节奏、纸墨关系，不能只展示一块颜色和主题名。
5. **密度是一组联动参数**：字号、行距、段距按“舒展 / 均衡 / 编辑部”成组变化，不让用户逐项猜数值。
6. **状态反馈安静但确定**：保存、选择、发布等状态使用位置、层级和短动效表达，避免堆叠彩色标签。
7. **令牌按角色命名**：`surface / text / stroke / action / motion` 比按色相命名更利于主题切换和维护。

## 本项目的原创实现

- 新增“校订 / 成稿”滑动工作视图；成稿视图只收起编辑标记，不改变正文或复制结果。
- 新增快速排版面板；集中承载版式骨架、阅读密度、纸墨气质，并保留进入完整样式设置的路径。
- 版式缩略图由项目自己的 Theme DSL 动态绘制，不复制 Mars 的主题卡、配色或组件结构。
- 继续使用中国红、深蓝、瓷白品牌语言；Mars 的陶土色、玻璃面板和桌面应用外壳不进入本项目。
- 动效遵循 140 / 220 / 360ms 三档节奏，支持 `prefers-reduced-motion`。

## 明确不借用

- 不复制 Mars 的品牌、火星图形、三栏文件树、Agent 面板、火星橙配色和桌面窗口结构。
- 不复制任何源码实现；仅转译信息架构、主题分层与状态表达原则。
- 不把本地文件系统与 CLI Agent 工作流伪装成当前 Web 版已有能力。

